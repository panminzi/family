# Frontend dev env. Copy to .env to override.

# Where the Vue app should call the backend during dev. Empty/relative means
# rely on Vite's /api proxy (configured in vite.config.ts).
VITE_API_BASE=/api
